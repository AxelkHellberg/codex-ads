---
name: ads-create
description: "Campaign brief generator that turns brand DNA and context into paid-media concepts."
---

# Ads Create

Use after `brand-profile.json` exists to produce `campaign-brief.md`.

```bash
python3 scripts/ads.py create --brand-profile brand-profile.json --output-dir .
```
